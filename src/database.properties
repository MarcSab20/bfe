# ============================================================
# Configuration de la base de données BFE
# ============================================================

# --- MySQL (par défaut) ---
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/bfe
db.username=marco
db.password=29Papa278.

# --- Oracle (décommenter pour utiliser Oracle) ---
# db.driver=oracle.jdbc.OracleDriver
# db.url=jdbc:oracle:thin:@localhost:1521:XE
# db.username=bfe_user
# db.password=bfe_password

# ============================================================
# Paramètres de pool de connexion
# ============================================================
db.pool.maxSize=10
db.pool.timeout=30000
